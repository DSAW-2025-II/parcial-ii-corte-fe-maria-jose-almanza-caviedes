// Copia este archivo a `src/config.js` y rellena la URL de tu backend cuando lo despliegues.
// Nunca subas `src/config.js` con datos secretos al repo.

window.APP_CONFIG = {
  API_BASE_URL: 'http://localhost:3000'
};
